You are working in an existing Java project that already implemented last week’s DB2 sample package.

That old implementation was based on:
- sample_case_verified-lastweek.zip
- the old prompt/rules
- the old 12-column relationship_detail.tsv format

Now you must UPGRADE the existing implementation to the LATEST sample package and the LATEST semantic contract.

====================================
SOURCE OF TRUTH FOR THIS TASK
====================================

Treat these as the ONLY source of truth for behavior:

1. latest sample SQL package:
   - sample_case_verified_3.zip
   - inside it, use:
     - sample_case_verified/table/
     - sample_case_verified/view/
     - sample_case_verified/function/
     - sample_case_verified/sp/
     - sample_case_verified/extra/

2. latest semantic contract:
   - relationship_detail.revised.md
   - if the uploaded file name is relationship_detail.md in the package root, use the latest revised semantics from that file / provided latest md artifact

3. latest golden expected direct-output:
   - relationship_detail.fixed.v2.tsv
   - if the uploaded file name is relationship_detail.tsv in the package root, use the latest fixed TSV / provided latest tsv artifact as the exact golden expected output

If the old implementation or the old prompt conflicts with the latest MD / latest TSV / latest sample scripts, ALWAYS follow the latest MD + latest TSV + latest sample scripts.

Do NOT keep old assumptions just because they worked last week.

====================================
GOAL
====================================

Update the existing Java project incrementally so that it generates ONLY:

- relationship_detail.tsv

for the latest sample package and matches the latest golden TSV exactly.

Do NOT implement lineage_path.tsv in this task.

Reuse the existing code and the existing main added last week as much as possible.
Prefer minimal invasive changes.
Do not break unrelated existing behavior.

====================================
VERY IMPORTANT DELTA FROM LAST WEEK
====================================

You must first inspect the current implementation and explicitly identify where it still reflects last week’s rules.

Major deltas from last week that MUST be applied:

1. OUTPUT COLUMN CHANGE
Last week TSV had 12 columns.
Latest TSV has 13 columns and includes:

- source_object_type
- source_object
- source_field
- target_object_type
- target_object
- target_field
- relationship
- line_no
- line_relation_seq
- line_content
- persistent_impact_objects
- intermediate_objects
- confidence

`line_relation_seq` is NEW and must be generated correctly.

2. NEW / CHANGED SOURCE OBJECT CONVENTIONS
- latest source_object_type includes:
  - VIEW_DDL
  - FUNCTION
  - PROCEDURE
  - SCRIPT
- latest extra files should use SCRIPT, not PROCEDURE
- latest view DDL source_object should be the actual view name, e.g.:
  - INTERFACE.V_API_MTM_REVAL
  not a generic file name like ddl_views.sql

3. NEW SQL FILES / NEW COVERAGE
Compared to last week, latest sample adds new files and patterns including:
- function/FN_WRAPPER.sql
- function/GET_MULTIPLIER.sql
- function/GET_TOP_DEALS.sql
- sp/ADVANCED_DML_PROC.sql
- sp/DEMO_PROC_DB2.sql
- sp/EDGE_CASES_PROC.sql
- sp/LOOP_DEMO_PROC.sql
- extra/DB2_EDGE_CASES_SCRIPT.sql

You must support the patterns these files introduce.

4. RELATIONSHIP SET CHANGED
Latest TSV / MD includes relationship types that did not exist last week, including:
- CREATE_FUNCTION
- CREATE_PROCEDURE
- VARIABLE_SET_MAP
- CURSOR_DEFINE
- CURSOR_READ
- CURSOR_FETCH_MAP
- FUNCTION_PARAM_MAP
- CALL_PARAM_MAP
- SPECIAL_REGISTER_MAP
- DIAGNOSTICS_FETCH_MAP
- FUNCTION_EXPR_MAP
- TABLE_FUNCTION_RETURN_MAP
- CONTROL_FLOW_CONDITION
- EXCEPTION_HANDLER_MAP
- DYNAMIC_SQL_EXEC
- MERGE_INTO

Do not omit these.

5. OLD RELATIONSHIP ASSUMPTIONS MUST BE REMOVED
The old implementation may still emit or assume older relationship names / rules such as:
- MERGE_TABLE
- MERGE_VIEW
- UPDATE_VIEW
- INSERT_VIEW
or other last-week-only behavior

Do NOT keep old relationship names if latest MD / latest TSV no longer use them.

6. INSERT_TARGET_COL RULE CHANGED
Latest MD now says:
- emit INSERT_TARGET_COL ONLY when the SQL explicitly has INSERT (col1, col2, ...)
- do NOT emit metadata-inferred INSERT_TARGET_COL rows just because target columns can be inferred
- do NOT generate line_content like:
  INFERRED TARGET COLUMN FROM METADATA: <COLUMN>
  unless the latest golden TSV actually contains such rows
- the implementation must follow latest MD and latest TSV, not last week’s inferred-column rule

7. SELECT LITERAL RULE CHANGED
For select-list literals such as:
- SELECT 'X'
use:
- SELECT_EXPR
not:
- SELECT_FIELD

8. EXACT SOURCE FIDELITY IS STRICT
- line_no must be the exact physical source line number in the original SQL file
- line_content must be the exact raw original source line from the file
- retrieve raw line_content from the original source file by line number
- do NOT reconstruct SQL from AST nodes
- do NOT trim leading spaces
- do NOT drop commas or semicolons
- do NOT normalize spacing

9. line_relation_seq RULE
This is now required.
It must be:
- partitioned by (source_object_type, source_object, line_no)
- starting from 0
- contiguous within each line group
- ordered by the natural SQL relationship order on that line according to the latest MD

This is NOT a whole-statement sequence across multiple lines.

10. UNKNOWN / DYNAMIC / UNSUPPORTED BEHAVIOR CHANGED
Different unresolved situations must not be conflated:
- EXECUTE IMMEDIATE should produce DYNAMIC_SQL_EXEC and/or UNKNOWN only as defined by latest MD / latest TSV
- unsupported proprietary commands like REORG TABLE should map to the latest unsupported placeholder behavior, not dynamic SQL placeholders
- do not reuse UNKNOWN_DYNAMIC_SQL for non-dynamic unsupported statements

11. LITERAL / SPECIAL TOKEN FORM
Use canonical source_field tokens such as:
- CONSTANT:'X'
- CONSTANT:2
- CONSTANT:CURRENT TIMESTAMP
- CONSTANT:SQLCODE
when required by the latest MD / latest TSV

12. CALL / FUNCTION PARAMETER MAPPING
Support:
- FUNCTION_PARAM_MAP
- CALL_PARAM_MAP

Rules:
- use formal parameter names when signature metadata is known from the sample object definition
- otherwise use positional fallback such as $1, $2 only when signature metadata is unavailable
- do not mix styles inconsistently for the same callable when metadata is known

13. RECORD-SLOT CURSOR MAPPING
For cursor / loop record variables, use precise slot targets where required by latest TSV, e.g. patterns like:
- V_ROW.DEAL_NUM
- V_ROW.TOTAL_AMT
- V_REC.DEAL_NUM
- V_REC.CUR_MTM_AMT
Do not collapse these to only the whole record variable if latest TSV is slot-level.

====================================
LATEST RELATIONSHIP TYPES TO SUPPORT
====================================

At minimum, the updated generator must support the relationship types actually used by the latest golden TSV:

- CALL_FUNCTION
- CALL_PARAM_MAP
- CALL_PROCEDURE
- CONTROL_FLOW_CONDITION
- CREATE_FUNCTION
- CREATE_PROCEDURE
- CREATE_TABLE
- CREATE_VIEW
- CREATE_VIEW_MAP
- CTE_DEFINE
- CTE_READ
- CURSOR_DEFINE
- CURSOR_FETCH_MAP
- CURSOR_READ
- DELETE_TABLE
- DELETE_VIEW
- DIAGNOSTICS_FETCH_MAP
- DYNAMIC_SQL_EXEC
- EXCEPTION_HANDLER_MAP
- FUNCTION_EXPR_MAP
- FUNCTION_PARAM_MAP
- GROUP_BY
- HAVING
- INSERT_SELECT_MAP
- INSERT_TABLE
- INSERT_TARGET_COL
- JOIN_ON
- MERGE_INSERT_MAP
- MERGE_INTO
- MERGE_MATCH
- MERGE_SET_MAP
- MERGE_TARGET_COL
- ORDER_BY
- RETURN_VALUE
- SELECT_EXPR
- SELECT_FIELD
- SELECT_TABLE
- SELECT_VIEW
- SPECIAL_REGISTER_MAP
- TABLE_FUNCTION_RETURN_MAP
- TRUNCATE_TABLE
- UNION_INPUT
- UNKNOWN
- UPDATE_SET
- UPDATE_SET_MAP
- UPDATE_TABLE
- UPDATE_TARGET_COL
- VARIABLE_SET_MAP
- WHERE

Only use relationship types that are defined by the latest MD and actually required by the latest sample / latest golden TSV.

====================================
PATTERNS INTRODUCED OR EXPANDED IN THE LATEST SAMPLE
====================================

You must support these latest patterns exactly as reflected in the latest golden TSV:

1. CREATE_FUNCTION and CREATE_PROCEDURE rows for function / procedure files
2. scalar function return expression:
   - RETURN P_FACTOR * 2;
   - operand-level RETURN_VALUE rows
3. wrapper function calling another function:
   - RETURN GET_MULTIPLIER(10);
   - CALL_FUNCTION
   - FUNCTION_PARAM_MAP
   - RETURN_VALUE callable dependency / expression dependency per latest TSV
4. table-valued function with FOR cursor loop and PIPE:
   - CURSOR_FETCH_MAP for loop record slots
   - SELECT_TABLE / SELECT_FIELD / SELECT_EXPR / GROUP_BY / HAVING / ORDER_BY
   - TABLE_FUNCTION_RETURN_MAP
5. cursor definition/open/fetch/close:
   - CURSOR_DEFINE
   - CURSOR_READ
   - WHERE CURRENT OF cursor
6. variable assignments:
   - VARIABLE_SET_MAP
7. function expression mapping inside assignments:
   - FUNCTION_EXPR_MAP
8. special register mapping:
   - CURRENT TIMESTAMP -> SPECIAL_REGISTER_MAP
9. diagnostics / exception handler mapping:
   - SET V_CODE = SQLCODE
   - handler for SQLEXCEPTION
   - DIAGNOSTICS_FETCH_MAP
   - EXCEPTION_HANDLER_MAP
10. control-flow condition usage:
   - IF P_ID > 100
   - WHILE V_COUNTER < P_LIMIT
   - CONTROL_FLOW_CONDITION
11. temp table definition:
   - DECLARE GLOBAL TEMPORARY TABLE SESSION.xxx
   - CREATE_TABLE with target_object_type = SESSION_TABLE
12. CALL procedure parameter mapping:
   - CALL_PARAM_MAP
13. EXECUTE IMMEDIATE:
   - DYNAMIC_SQL_EXEC and confidence according to latest MD / latest TSV
14. unsupported DB2 utility statement:
   - REORG TABLE ...
   - UNKNOWN / unsupported placeholder behavior according to latest MD / latest TSV
15. extra script with select literal:
   - SELECT 'X' FROM T1 JOIN T2 ...
   - SELECT_EXPR
   - JOIN_ON
   - HAVING fallback behavior per latest TSV

====================================
WORK IN THIS ORDER
====================================

1. Inspect the current project structure first
2. Inspect the current implementation that was added last week
3. Identify exactly which old rules are still hardcoded
4. Inspect the latest sample package structure
5. Inspect the latest relationship_detail MD carefully
6. Inspect the latest golden TSV carefully
7. Summarize the delta from last week in your own words before coding
8. Then make minimal incremental code changes
9. Generate the latest relationship_detail.tsv
10. Compare generated TSV against the latest golden TSV
11. Refine until the output matches exactly

Do not skip the comparison step.

====================================
CLI / MAIN
====================================

Reuse the dedicated relationship-detail main added last week if possible.

Keep CLI support for:
- --tableDir
- --viewDir
- --functionDir
- --spDir
- --outputDir
- --extraDir   optional

Do not redesign the whole application.
Do not break the existing main entry point.

====================================
MATCHING REQUIREMENT
====================================

Your generated output for the latest sample must match the latest golden TSV exactly in:
- columns
- relationship names
- source_object_type
- source_object
- source_field
- target_object_type
- target_object
- target_field
- line_no
- line_relation_seq
- line_content
- persistent_impact_objects
- intermediate_objects
- confidence
- row ordering

Use the latest golden TSV as the acceptance test.

====================================
ORDERING REQUIREMENT
====================================

Maintain stable row ordering based on:
1. source object traversal order
2. source line order
3. within the same (source_object_type, source_object, line_no), natural SQL order via line_relation_seq

Do not use arbitrary map iteration order.

====================================
VALIDATION / DIFF REQUIREMENT
====================================

After implementation, run a strict comparison against the latest golden TSV and report:
- missing rows
- extra rows
- rows with same key but different line_no / line_relation_seq / line_content / persistent / intermediate / confidence
- relationship-name mismatches
- ordering mismatches

Also print a short summary:
- generated row count
- expected row count
- exact match: yes/no

====================================
IMPLEMENTATION STYLE
====================================

- inspect first, code second
- reuse existing scanner / parser / writer logic as much as possible
- prefer incremental enhancement
- keep unsupported cases explicit
- avoid broad refactors
- write readable code
- add focused comments only where useful

====================================
DELIVERABLES
====================================

Please provide:
1. the minimal code changes needed
2. any updated model / enum / writer changes for the 13-column format
3. any extractor changes required for the new relationships
4. the final generated relationship_detail.tsv
5. a diff summary versus the latest golden TSV
6. brief notes on which old last-week assumptions were removed

IMPORTANT:
- Do NOT follow last week’s prompt where it conflicts with the latest MD / TSV
- The latest MD + latest TSV + latest sample scripts override the old behavior
- Do NOT implement lineage_path.tsv in this task
